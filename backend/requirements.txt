# FastAPI and web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Vector store
qdrant-client==1.7.0
neo4j>=5.0.0

# ML and embeddings — pin torch/scipy before sentence-transformers (avoids pip backtracking on aarch64 Docker).
# torch 2.1.x wheels are no longer published for some Python/platform combos (e.g. newer CPython); 2.2.x is the practical floor.
# huggingface_hub>=0.20: accelerate (via unstructured_inference) needs split_torch_state_dict_into_shards
torch==2.2.2
torchvision==0.17.2
scipy==1.11.4
sentence-transformers==2.2.2
huggingface_hub>=0.20.0,<0.26.0
# transformers 4.40+ expects newer hub APIs; keep below 4.40 with hub range above
transformers>=4.30.0,<4.40.0
tiktoken==0.5.2
numpy==1.26.3

# PDF processing
pymupdf==1.23.8

# LLM clients
openai==1.10.0
httpx==0.26.0

# Configuration
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# OSINT additions - document parsing (0.12.0 requires Python <3.12; 0.16.x supports 3.12)
unstructured[pdf,docx,html,email]>=0.16.0,<0.17

# OSINT additions - OCR
pytesseract==0.3.10
pdf2image==1.16.3

# OSINT additions - NLP/entity extraction
spacy==3.7.4

# Caching and rate limiting
redis==5.0.1
slowapi==0.1.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
python-dateutil==2.8.2
aiofiles==23.2.1
pandas>=2.0.0
requests>=2.28.0

# BM25 for fusion retrieval (import: rank_bm25)
rank-bm25>=0.2.2
